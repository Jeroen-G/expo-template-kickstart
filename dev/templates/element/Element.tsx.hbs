import React from 'react';
import { StyleSheet } from 'react-native';

type Props = {
  children: React.ReactNode;
};

export default function {{pascalCase name}}({children}: Props): React.ReactElement {
  return (<{{pascalCase name}}>{children}</{{pascalCase name}}>);
};

const styles = StyleSheet.create({});
