import React from 'react';
import { StyleSheet, Text } from 'react-native';

import { Screen } from '../../../elements/Screen';

export default function {{pascalCase name}}(): React.ReactElement {
  return (
    <Screen>
      <Text>
        {{pascalCase name}}
      </Text>
    </Screen>
  );
};

const styles = StyleSheet.create({});
