import React from 'react';
import { StyleSheet } from 'react-native';

type Props = {};

export default function {{pascalCase name}}({}: Props): React.ReactElement {
  return ();
};

const styles = StyleSheet.create({});
